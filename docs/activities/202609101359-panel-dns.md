# Panel public hostname — 2026-09-10

## Request and starting state
Create panel.sufficit.com.br for the panel already deployed on Eveo. No panel DNS
record or matching Nginx virtual host existed. Reused the DNS repository's Route53
credential/zone pattern without printing credentials. Kept the existing Unix socket
and /telephony-panel path; no application rebuild was necessary.

## Changes
- Route53 A ALIAS panel.sufficit.com.br -> eveo-apps.sufficit.com.br, health
  evaluation enabled. Change C08460221DAGGGJQ159GE. No other DNS record overwritten.
- Added sufficit-services-dns/create_panel_dns.py: read-only by default, explicit
  --apply, CREATE only and refusal to overwrite differing existing records.
- New deploy/nginx-panel-domain.conf uses existing wildcard certificate and panel
  snippet. HTTP redirects to HTTPS; HTTPS root redirects to /telephony-panel/.
- Added hostname to AllowedHosts in source and deployed systemd environment.
- Restarted ONLY sufficit-telephony-panel. Nginx was gracefully reloaded after
  nginx -t succeeded. No PBX changes or commands.
- Updated both local Identity client definitions to include the new callback while
  retaining the original Eveo callback; no Identity provisioning request was made.

## Verification
- Authoritative ns-408.awsdns-51.com, Cloudflare 1.1.1.1 and Google 8.8.8.8 return
  177.104.185.118. Script second inspection confirms exact alias, no mutation.
- HTTPS certificate verification=0; root=302 to /telephony-panel/; HTTP=308 to HTTPS.
- Page and /health return 200; anonymous /api/fleet returns 401.
- Browser loads the correct title and login gate at the new HTTPS hostname.
- Nginx configuration validates without warnings; local git diff --check passes.
- Unchanged pre/post PIDs: portal 1534607, Identity 2400961, observation 1558094.
  Panel changed from 2396460 to 2451350 to apply its host allowlist.

## Known remaining work
This completes only the requested DNS/HTTPS entry point. Real manager login still
depends on authorized Identity provisioning recorded in PLAN-telephony-panel.md.
No security bypass, customer grant, commit or push was made. The broader plan is
intentionally retained. Development and browser skills guided recording and route
validation; no visual redesign was needed for this hostname change.
