# Canais técnicos não são ramais — 10/09/2026 18:46 -03

## Diagnóstico e correção

A captura mostrava dois cartões `Local/0000006001@sufficit-pjsip...` na seção de
ramais. `AuthorizedObservation.ForManager` gerava PEER para todo evento de canal;
a retenção já excluía Local, mas a visualização atual não fazia essa distinção.

Versão 0.8.1 separa dados diagnósticos de identidades: somente SIP/PJSIP/IAX2
geram cartões de endpoint no fluxo central. Canais técnicos Local continuam em
Rows, Canais e correlação de chamadas, sem cartões de ramal independentes.
Normalização reutiliza `BoardRuleEngine.EndpointKey` e só remove sufixos de oito
hexadecimais em canais. As regras FOP continuam classificando endpoints e troncos.
Nenhum novo filtro configurável, alteração visual, permissão ou limpeza foi criado.
A skill software-development orientou plano, regressões e entrega; o harness de
frontend existente verificou o comportamento sem redesenhar a interface SUI.

## Validação

- 153 verificações: `dotnet run --project tests/Panel.Tests.csproj -m:1 -v:q`.
- Novas regressões: Local isolado/múltiplos legs, preservação dos três canais da
  chamada, fusão de peer+canal real, canais técnicos sem PEER, sufixo não hexadecimal,
  tecnologia IAX2 preservada.
- Browser: `operator-board.browser.cjs`, `board-inventory.browser.cjs` e
  `call-grouping.browser.cjs`, usando NODE_PATH=/tmp/k8sui-edit/node_modules.
  Ramais sem cartões Local, Canais com os dois legs, retenção e correlação intactas;
  desktop/móvel/temas e teclado passaram. Dados de demonstração, não sessão real.
- Publish Release e `git diff --check` concluídos sem erros.

## Deploy

Somente Eveo painel: `/opt/sufficit-telephony-panel/releases/channel-cards-20260910-2146`.
Assembly SHA256 `188dd177a435fa4090a279c8043f468db5adcf0c11b38f0fa792b03f4bea7bf2`.
Configurações privadas do host copiadas para o release sem expor conteúdo.
Regras e inventário não foram sobrescritos nem limpos. Inventário existente 0600,
sufftelpanel, 263623 bytes ao verificar. O hash de regras mudou entre preflight e
pós-deploy, fora dos comandos desta entrega; não foi revertido.

Painel PID 2704609 ativo, NRestarts 0; health 200 e API anônima 401; socket gRPC
presente, sem erros no journal na janela inspecionada. Observer PID 2618869 e AMI
PID 2619544 inalterados. Nenhum PBX, Identity ou coletor reiniciado.

Sem commit/push nesta solicitação. Mudanças e planos preexistentes preservados.
Sessão humana autenticada não foi exercitada; atualizar a página carrega a correção.
