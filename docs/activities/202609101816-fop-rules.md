# Importação de troncos FOP

Pedido: incluir as mesmas regras do FOP no painel. Preflight confirmou catálogo
remoto ausente. Fonte somente writeTrunks do controlador indicado pelo usuário.

Entregue: [catálogo](../../deploy/fop-board-rules.json) com 14 grupos/130 padrões,
UUIDv7, protocolos e ordem preservados. Prefixos in-/out explicitados, duplicata
interna removida e sobreposição entre INBOUND/MIXED documentada. Nenhuma senha,
permissão, contexto de discagem ou configuração PBX importada.

Limite ampliado de 32 para 64 padrões por regra (INBOUND tem45), teto512 mantido.
UI atualizada no texto e maxlength. Ferramenta administrativa BoardRules adicionada
com extração restrita, merge idempotente, recusa de conflitos/revisão e verificação.

## Validação

- 121 checks .NET passaram, incluindo catálogo, fallbacks, ordem, ausência de
  conversão SIP/PJSIP, IAX2, limite64/65, merge e credenciais excluídas.
- Catálogo comparado à extração atual da fonte, idêntico exceto IDs gerados.
- Publish Release painel0.7.1 e ferramenta, sem erros; git diff --check limpo.
- Nenhuma nova verificação de navegador autenticado; a mudança visual foi somente
  helper/maxlength, sem mudança de composição. Releitura pelo store no host validada.

## Execução no Eveo

Release `/opt/sufficit-telephony-panel/releases/fop-20260910-2130`, DLL SHA-256
`be6a31bc4cdaaeebe976c6f61fd10ac8b3413e7d0f8c7604485c6092da684544`.
Configurações privadas preservadas no host. Parada breve apenas do painel,
com trap de start em erro; importação offline como sufftelpanel evita corrida com
o cache do processo. Preflight de ausência revalidado pelo importador.

Arquivo `/var/lib/sufficit-telephony-panel/board-rules.json` pertence a sufftelpanel,
modo0600. Revisão `01a08d2e077d75fd8cf3143a712d6bf8`; releitura14grupos/130padrões.
Painel PID2678267 ativo/NRestarts0; health200, fleet anônimo401. Observador
PID2618869 e AMI PID2619544 mantidos. Nenhum Asterisk/Identity/Nginx alterado.

Documentação: [importação e limites](../fop-rule-import.md). Catálogo local é a
semente importada, não sincronização automática futura. Alterações da UI ficam
no arquivo privado do host. Rollback0.7.0 não aceita45 padrões e exige tratamento
explícito. Nenhum backup adicional, commit ou push; worktree anterior preservado.
